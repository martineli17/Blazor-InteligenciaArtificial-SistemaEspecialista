@page "/variavel"
<Cabecalho NomeAba="Variáveis" />
<_Novo OnRefreshList="RefreshListagem" />
<hr />
<ListVariavel Atualizar="AtualizarListagem" />

@code {
    private bool AtualizarListagem;

    private void RefreshListagem()
    {
        AtualizarListagem = !AtualizarListagem;
        StateHasChanged();
    }
}